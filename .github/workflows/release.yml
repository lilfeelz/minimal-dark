name: release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  package:
    permissions:
      contents: write
    uses: JakobMelchard/.github/.github/workflows/vscode.yml@main
    with:
      release: true
